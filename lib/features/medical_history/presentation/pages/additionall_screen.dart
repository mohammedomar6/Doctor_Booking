import 'package:flutter/material.dart';

class AdditionallScreen extends StatelessWidget {
  const AdditionallScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:  AppBar(title: Text('Additionall files'),),

    );
  }
}
